// Package memfs implements a basic in-memory filesystem.
//
// Although this package should be something located
// `examples/memfs`, we want a fully functional filesystem
// when testing in `winfsp_test.go`, and the in-memory
// filesystem is the chosen one.
package memfs
